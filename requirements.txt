# Core dependencies
pypdf>=5.1.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
qdrant-client>=1.7.0
sentence-transformers>=2.2.0

# Text generation
transformers>=4.30.0
torch>=2.0.0
accelerate>=0.20.0

# Optional: for quantization support (8-bit/4-bit loading)
# bitsandbytes>=0.41.0

# Google Gemini
google-genai

# Local model server (OpenWebUI/OpenUI integration)
fastapi>=0.95.0
uvicorn[standard]>=0.22.0

# Optional: for CORS in OpenWebUI or browser-based clients
python-multipart>=0.0.5
